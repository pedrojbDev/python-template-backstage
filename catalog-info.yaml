apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ubiq-backstage
  description: An example of a Backstage application.
  # Example for optional annotations
  # annotations:
  #   github.com/project-slug: backstage/backstage
  #   backstage.io/techdocs-ref: dir:.
spec:
  type: website
  owner: example.com
  lifecycle: experimental

---

apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: templates
  description: Registro de templates disponíveis no Backstage.
spec:
  targets:
    # Registro do template Python, Go e Rust
    - ./templates/python-go-rust-template.yaml

   
